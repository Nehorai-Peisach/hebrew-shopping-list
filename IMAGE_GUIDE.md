# תמונות ברשימת הקניות - מדריך פתרון בעיות

## כיצד להשתמש בתכונת התמונות

### הוספת פריט עם תמונה
1. לחץ על כפתור ה- "+" או "הוסף פריט"
2. בדיאלוג שמופיע, לחץ על "בחר תמונה"
3. בחר תמונה מהגלריה שלך
4. לאחר בחירת התמונה, ללחץ שוב "בחר תמונה" כדי לאשר
5. הזן שם פריט וכמות
6. לחץ "הוסף"

### עריכת תמונה של פריט
1. לחץ על הפריט לעריכה
2. בדיאלוג העריכה, לחץ על "שנה תמונה"
3. בחר תמונה חדשה
4. לחץ "עדכן"

## בעיות נפוצות וכיצד לפתור אותן

### תמונות לא מופיעות
**סיבות אפשריות:**
1. Firebase Storage לא מוגדר כראוי
2. אין אינטרנט כשה-App עובד
3. קובץ ה-storage.rules לא התעדכן

**פתרון:**
1. בדוק את ה-Console (F12) אם יש שגיאות
2. בדוק אם ה-Firebase Storage enabled בפרויקט
3. וודא שה-storage.rules עודכנו עם הרשאות קריאה

### תמונה לא מתעלית
**סיבות אפשריות:**
1. גודל הקובץ גדול מדי
2. סוג הקובץ לא נתמך (צריך PNG/JPG)
3. בעיית אינטרנט

**פתרון:**
1. נסה עם קובץ תמונה קטן יותר
2. נסה JPG או PNG
3. בדוק את החיבור לאינטרנט

### אני רואה שגיאה בקונסול
בדוק את Console (F12 > Console) עם שיקום לאחד מהדברים הבאים:

**"No provider found for Storage"**
- וודא שה-main.ts עודכן עם `provideStorage`

**"Access denied" בעת קריאה לתמונה**
- עדכן את storage.rules כדי לאפשר גישה

**"Failed to upload image"**
- בדוק אם יש מספיק מקום ב-Storage
- בדוק אם ה-userId מעוברת כראוי

## טיפים לשימוש

1. **הקטן תמונות** - תמונות קטנות מתעלות מהר יותר
2. **השתמש ב-JPG** - קטן יותר מ-PNG בדרך כלל
3. **בדוק אותך מחוברת** - תעלוי דורש אינטרנט

## עבור מפתחים

### איפה מאוחסנות התמונות?
- Firebase Storage בנתיב: `items/{userId}/{listId}/{itemId}`

### איפה מאוחסנות הפרטים?
- Firestore collection: `items`
- שדה: `imageUrl` (מכיל את ה-download URL מ-Firebase Storage)

### איך לבדוק בדירוג הפיתוח?
```javascript
// בקונסול:
firebase.storage().bucket().list({prefix: 'items/'})
```
